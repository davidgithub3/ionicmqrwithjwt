import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.study.mqrproj',
  appName: 'ionicmqr',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
